import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "JECP - Jagriti Enterprise Centre Purvanchal | Building India Through Enterprise",
  description: "JECP is a cutting-edge incubation center dedicated to empowering small and medium enterprises across Tier 2 and 3 districts of India. Every small idea can carry the weight of a nation!",
  keywords: "JECP, Jagriti, Enterprise, Incubation, Entrepreneurship, Purvanchal, India, SME, Startup",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <Header />
        <main className="pt-20">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
